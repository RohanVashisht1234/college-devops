input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  mutate {
    add_field => { "platform" => "medgenome" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "medgenome-api-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
